import { Event, EventType, EventPrivacy, EventStatus } from "./types";

// This project no longer includes example event data. Replace these exports
// with real client data (from your backend or management UI) when onboarding
// a client. Keeping these empty avoids shipping mock content to production.

export const ROYALTY_FREE_TRACKS: any[] = [];

export const INITIAL_EVENTS: Event[] = [];
